# Lecture topics to develop

- [ ] **Niche category** — create `lessons/niche/` for narrow-audience items; `omnigraffle-tools` goes there. (`dedup-video`: omitted entirely. Statusline / settings.json small items: still undecided. codex-MCP setup → became Lesson 17.)
- [ ] **Set `$GH` on Linux accounts** — the env var only exists on Macs today (see Lesson 2). User action.
- [ ] **Meta-lesson** — document the process of building this collection and the lecture itself, in detail, as a final example.
- [ ] **Review the reviews → an error taxonomy of the model** — everything is written by one model, so its errors are systematic, and the review corpus (codex/sonnet/opus rounds across ~196 sessions / ~1,717 transcripts on disk, all projects — *not* one repo) is a labeled dataset of those mistakes. Mine it for recurring failure classes and ship a *targeted review* per high-frequency class — the way Lesson 03 turned "never silently skip" into a dedicated audit that actually runs. Deliverable open (a `notes/` sweep → new lesson(s) and/or per-class audit skills). Dedicated session; the meta-classification itself needs a decorrelated lens (codex/owner), since Claude analyzing its own errors shares their blind spots. Full scope + method + candidate families + caveats: [`notes/review-corpus-error-taxonomy.md`](notes/review-corpus-error-taxonomy.md).
