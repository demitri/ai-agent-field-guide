# Lecture topics to develop

- [ ] **Multi-agent, cross-repo, GitHub-issue protocol** (user-flagged, priority) — the working protocol should be documented "as a thing that is visible." Collect the full story from the user first: repos involved, how issues carry work between agents, the lunchtime worker's role, and what "visible" should mean.
- [ ] **Niche category** — create `lessons/niche/` for narrow-audience items; `omnigraffle-tools` goes there. (`dedup-video`: omitted entirely. Statusline / settings.json / codex-MCP small items: still undecided.)
- [ ] **Set `$GH` on Linux accounts** — the env var only exists on Macs today (see Lesson 2). User action.
- [ ] **Meta-lesson** — document the process of building this collection and the lecture itself, in detail, as a final example.
